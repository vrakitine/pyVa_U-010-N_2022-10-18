def setup(local):

    ### The  variables setting:

    local.defineVariable('Input array...M', [])
    local.defineVariable('Index of current element of array...i_main', -1)
    local.defineVariable('The current element of array...current element', 0)